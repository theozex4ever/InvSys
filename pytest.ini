[pytest]
# Tell pytest where to find test files.  Without this, pytest searches the
# entire project tree which can pick up unrelated files or be slow.
testpaths = tests

# Display a short summary of all failures at the end of the run.
# 'a' = all: shows passed, failed, errors, skipped in one glance.
addopts = -v --tb=short
